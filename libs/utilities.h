/* sticky press
if rex_space_press_since_release_times > KEY_HOLD_THRESHOLD, pressing key behavior will be judged as REX_KEY_HOLD
else REX_KEY_PRESS
*/
#define KEY_HOLD_THRESHOLD 3
/* max buffer size to store digits then draw them in a row */
#define MAX_DIGITS_AMOUNT 10
#define DRAW_NUMBER_DIGIT_OFFSET 20
